name: Deploy no Servidor - Teste
on:
    push:
        branches: 
            - main

jobs:
    deploy:
        runs-on: self-hosted
        steps:
            - name: Checkout do Código
              uses: actions/checkout@v4
            - name: Construir containers
              run: docker-compose up -d --build